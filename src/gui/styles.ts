import {css} from 'lit-element';

export const colors = css`
  :host {
    --bg: #222;
    --white: #eee;
    --accent: #3374ea;
  }
`;

export const fonts = css`
  * {
    font-family: sans-serif;
    color: var(--white);
  }
`;
